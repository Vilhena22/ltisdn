info:
  name: Remover IP da Interface
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{baseURL}}/ip/address/*6"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
