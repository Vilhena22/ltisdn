info:
  name: Get IP Address
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseURL}}{{address}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
