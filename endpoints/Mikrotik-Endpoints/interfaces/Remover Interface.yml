info:
  name: Remover Interface
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseURL}}/interface/*B"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
