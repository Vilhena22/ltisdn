info:
  name: Des/Ativar Interface
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{baseURL}}/interface/*13"
  body:
    type: json
    data: |-
      {
        "disabled": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
