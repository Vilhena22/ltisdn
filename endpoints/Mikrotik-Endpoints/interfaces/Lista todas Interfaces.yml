info:
  name: Lista todas Interfaces
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseURL}}/interface"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
