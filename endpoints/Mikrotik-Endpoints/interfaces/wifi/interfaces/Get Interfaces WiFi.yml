info:
  name: Get Interfaces WiFi
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseURL}}/interface/wireless"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
