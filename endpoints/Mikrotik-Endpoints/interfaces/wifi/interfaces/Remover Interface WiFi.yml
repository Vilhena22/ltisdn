info:
  name: Remover Interface WiFi
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{baseURL}}/interface/wifi/*14"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
