info:
  name: Remover Perfil Seguranca
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{baseURL}}/interface/wifi/security/*2"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
