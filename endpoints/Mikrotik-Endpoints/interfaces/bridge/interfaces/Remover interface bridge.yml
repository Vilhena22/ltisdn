info:
  name: Remover interface bridge
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{baseURL}}/interface/bridge/ID"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
