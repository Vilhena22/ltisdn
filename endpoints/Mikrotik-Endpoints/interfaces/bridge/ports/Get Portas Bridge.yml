info:
  name: Get Portas Bridge
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseURL}}/interface/bridge/port"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
