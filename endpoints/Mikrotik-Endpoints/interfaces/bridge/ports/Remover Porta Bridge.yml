info:
  name: Remover Porta Bridge
  type: http
  seq: 2

http:
  method: DELETE
  url: "{{baseURL}}/interface/bridge/port/*3"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
