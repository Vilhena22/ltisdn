info:
  name: Atualizar Sistema
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseURL}}/system/package/update"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
