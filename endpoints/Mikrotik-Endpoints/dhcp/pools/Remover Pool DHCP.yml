info:
  name: Remover Pool DHCP
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{baseURL}}/ip/dhcp-server/network/remove"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
