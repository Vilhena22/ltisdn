info:
  name: Remover Servidor DHCP
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseURL}}/ip/dhcp-server/remove"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
