info:
  name: "Get Servidores DHCP "
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseURL}}/ip/dhcp-server"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
