info:
  name: Desativar Servidor DHCP copy
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseURL}}/ip/dhcp-server/disable"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
