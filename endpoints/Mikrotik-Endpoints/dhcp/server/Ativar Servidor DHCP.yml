info:
  name: Ativar Servidor DHCP
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseURL}}/ip/dhcp-server/enable"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
