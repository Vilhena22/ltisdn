info:
  name: Get Clientes DHCP
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseURL}}/ip/dhcp-client"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
