info:
  name: Adicionar Novo Cliente DHCP
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseURL}}/ip/dhcp-client/add"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
