info:
  name: Remover Cliente DHCP
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseURL}}/ip/dhcp-client/remove"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
