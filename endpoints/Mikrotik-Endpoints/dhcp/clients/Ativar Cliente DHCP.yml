info:
  name: Ativar Cliente DHCP
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseURL}}/ip/dhcp-client/enable"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
