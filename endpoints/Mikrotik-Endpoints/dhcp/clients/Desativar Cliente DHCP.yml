info:
  name: Desativar Cliente DHCP
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseURL}}/ip/dhcp-client/disable"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
