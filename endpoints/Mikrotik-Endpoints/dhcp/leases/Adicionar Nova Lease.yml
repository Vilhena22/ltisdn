info:
  name: Adicionar Nova Lease
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseURL}}/ip/dhcp-server/lease/add"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
