info:
  name: Get Leases DHCP
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseURL}}/ip/dhcp-server/lease"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
