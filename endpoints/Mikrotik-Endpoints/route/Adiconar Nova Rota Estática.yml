info:
  name: Adiconar Nova Rota Estática
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseURL}}/ip/route/add"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
