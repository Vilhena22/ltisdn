info:
  name: Remover Rota Estática
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{baseURL}}/ip/route/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
