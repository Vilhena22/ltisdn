info:
  name: Get Rotas Estáticas
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseURL}}/ip/route"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
