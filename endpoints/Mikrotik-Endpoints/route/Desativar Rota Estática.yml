info:
  name: Desativar Rota Estática
  type: http
  seq: 5

http:
  method: POST
  url: "{{baseURL}}/ip/route/disable"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
