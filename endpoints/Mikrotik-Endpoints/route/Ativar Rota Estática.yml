info:
  name: Ativar Rota Estática
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseURL}}/ip/route/enable"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
