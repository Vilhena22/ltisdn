info:
  name: Configurar Servidores DNS
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseURL}}/ip/dns/set"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
