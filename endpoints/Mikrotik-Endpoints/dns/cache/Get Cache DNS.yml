info:
  name: "Get Cache DNS "
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseURL}}/ip/dns/cache"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
