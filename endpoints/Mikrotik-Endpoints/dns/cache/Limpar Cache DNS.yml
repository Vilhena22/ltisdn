info:
  name: Limpar Cache DNS
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseURL}}/ip/dns/cache/flush"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
