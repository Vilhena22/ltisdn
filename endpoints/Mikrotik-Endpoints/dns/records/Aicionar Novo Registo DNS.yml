info:
  name: Aicionar Novo Registo DNS
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseURL}}/ip/dns/static/add"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
