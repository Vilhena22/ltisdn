info:
  name: Romver Registos DNS
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{baseURL}}/ip/dns/static/remove"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
